/**
 * Drop configuration is intentionally empty.
 *
 * Frogments use the flat deterministic roller in `shared/drops/logic.ts` and
 * no longer have rarity-based tables, shard types, or combination costs.
 */
export const FROGMENT_ITEM = "frogment" as const;
